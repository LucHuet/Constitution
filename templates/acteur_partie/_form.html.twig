{{ form_start(form, {
    'attr': {
        'class': 'form-inline js-new-acteur-form',
        'novalidate': 'novalidate'
    }
}) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
