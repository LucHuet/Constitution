{% extends 'base.html.twig' %}

{% block title %}ActeurPartie{% endblock %}

{% block body %}
    <h1>ActeurPartie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ acteur_partie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ acteur_partie.nom }}</td>
            </tr>
            <tr>
                <th>NombreIndividus</th>
                <td>{{ acteur_partie.nombreIndividus }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('acteur_partie_index') }}">back to list</a>

    <a href="{{ path('acteur_partie_edit', {'id': acteur_partie.id}) }}">edit</a>

    {{ include('acteur_partie/_delete_form.html.twig') }}
{% endblock %}
